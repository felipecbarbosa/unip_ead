#include <stdio.h>
#include <stdlib.h>

int main () // tipo int = retornar um número inteiro /* */
{           // Diretivas do pré-processador
			// = comentários
			// Estrutura de um programa e linguagem C
	// Declaração de variáveis
	// Instrução 1
	// Instrução 2
	// ..............
	// Instrução(n)
	printf("Estrutura em linguagem C\n");
	printf("Adicionando Instrução de programa\n"); // Códigos especiais "\n"
	printf("include <stdio.h>\n");
	printf("include <stdlib.h>\n");
	system("PAUSE"); // Executa um comando interno do sistema operacional (.EXE, .COM, .BAT)
	return 0; // return var-tipo
}
// Inserir espaços em branco = tabulação e pular linhas à vontade em seus programas.
// O compilador ignora esses caracteres;
// Instruções de programa #include <stdio.h> e #include <stdlib.h>
